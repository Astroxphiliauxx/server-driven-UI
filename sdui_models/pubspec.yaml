name: sdui_models
description: Shared models for Server-Driven UI - used by both server and client.
version: 1.0.0

environment:
  sdk: ^3.8.1

dependencies:
  freezed_annotation: ^2.4.1
  json_annotation: ^4.8.1

dev_dependencies:
  build_runner: ^2.4.8
  freezed: ^2.4.7
  json_serializable: ^6.7.1
  lints: ^5.0.0
  test: ^1.24.0
